# Casper CDP (Casper Testnet) example env file
#
# This repo primarily uses shell exports (see `ORDER.md` / `DEPLOY.md`), but
# keeping a local `.env` can be convenient. Do NOT commit `.env`.
#
# Usage (bash/zsh):
#   set -a
#   source .env
#   set +a

# Network parameters (confirm via docs/casper/decision/parameter-confirmation.md)
CSPR_DECIMALS=9
SCSPR_DECIMALS=9

# External dependency contracts (must be confirmed; do not guess)
SCSPR_TOKEN_HASH=hash-<your-stcspr-cep18-token-hash>
SCSPR_LST_HASH=hash-<your-stcspr-lst-adapter-hash>

# Optional: deploy LST (stCSPR ybToken + WithdrawQueue) as well
DEPLOY_LST=true

# Optional: key paths for wrapper scripts (never commit real keys)
CSPR_CDP_DEPLOY_KEY=/path/to/secret_key.pem
